const ytdl = require("ytdl-core");
const fs = require ('fs');

async function youtube(link) {
  try{
  }catch (err) { 
    console.log(err)
    return { kode: 0 }
  }
  
}; 

module.exports ={
  youtube
}